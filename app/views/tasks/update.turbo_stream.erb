<%= turbo_stream.replace "task_#{@task.id}", partial: "tasks/task", locals: { task: @task } %>
<%= turbo_stream.remove "todo_task_#{@task.id}" %>
<%= turbo_stream.prepend "flash", partial: "layouts/flash" %>
